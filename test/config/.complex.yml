any-key: any-value
expanded-key: ${aws-secrets-manager.secret}
number-content: 1234
boolean-content: yes
json-content: |
  {
    "host": "localhost"
  }

aws-secrets-manager:
  secret: secret-id

aws-parameter-store:
  secret: parameter-name
